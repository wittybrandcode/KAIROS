# 🚀 دليـل تسليـم المـشـروع: نظام كـايروس (Kairos Handoff Guide)

مرحباً بك أيها المطور في **نظام كايروس (Kairos Design System)**. هذا المستند تمت كتابته خصيصاً لك لكي تفهم الهندسة العميقة، المنهجية الصارمة، وهيكلية الملفات لتتمكن من إكمال العمل بسلاسة وبنفس الرؤية دون إحداث أي خلل في المعمارية.

---

## 🛑 1. الفلسفة والرؤية (Vision & Philosophy)
"كايروس" ليس مجرد مكتبة واجهات عادية، بل هو **نظام تصميم مخصص لمعدات البث التلفزيوني (Broadcast Control Surfaces)** مثل (Switchers, Audio Consoles). 
- **المعمارية النقية:** نواة كايروس مبنية بـ **(HTML + CSS + Vanilla JS)** نقي فقط. يمنع منعاً باتاً دمج أطر عمل مثل React أو Vue داخل نواة الإطار، لضمان أن يكون الإطار مستقلاً (Framework-Agnostic) قابلاً للعمل في أي تقنية.
- **القسوة والصرامة:** لا يوجد `border-radius`. كل الزوايا حادة. الألوان دلالية ومدروسة هندسياً (PGM, PVW, Info, Warning).

---

## 📁 2. أين تجد الملفات؟ (Project Structure)

المشروع ينقسم إلى شقين رئيسيين: الشق الأول هو **النواة (Core)**، والشق الثاني هو **منصة العرض (Showcase)**.

### الشق الأول: النواة (The Framework Core)
مكان العمل الأساسي لتطوير الـ CSS. موجود في مجلد `src/`:
- `src/foundation/`: يحتوي على رموز التصميم (Tokens) مثل الألوان، المسافات، والخطوط (`typography.css`, `spacing.css`, إلخ).
- `src/utilities/`: الكلاسات المساعدة (`utilities.css`). **تنبيه:** نستخدم سلّماً دلالياً صارماً (مثال: `.kairos-p-standard` و `.kairos-gap-loose`) ويمنع استخدام أسماء كلاسات تشبه Tailwind (مثل `p-4` أو `gap-sm`).
- `src/components/`: المكان الذي نكتب فيه تنسيقات المكونات العامة (مثل الأزرار `buttons.css` وحقول الإدخال). المكونات تعتمد مبدأ "عقود المسافات" (CSS Contracts).
- `src/domain/`: تخطيطات مخصصة لعالم البث التلفزيوني (مثل الشاشات المتعددة Multiview وهيكل الواجهة Shell).

### الشق الثاني: منصة العرض والاختبار (The Showcase Dashboard)
نحن نقوم ببناء واجهة توثيق لعرض المكونات واختبارها (أشبه بـ Storybook لكنه مخصص وخفيف).
- `index.html`: الملف الرئيسي لمنصة العرض. تم بناؤه **باستخدام كلاسات وأزرار كايروس نفسها** (Dogfooding).
- `showcase.js`: المحرك الذي يدير الواجهة. يحتوي على مصفوفة `KairosInventory` التي تضم أسماء المكونات. يقوم هذا المحرك بجلب المكونات عبر `fetch` وحقنها في الصفحة لتجنب تكرار الكود.
- `showcase-data/`: المجلد الذي يحتوي على ملفات `HTML` المستقلة لكل مكون (مثل `buttons.html`, `typography.html`).

---

## ⚙️ 3. بيئة التطوير والتشغيل (How to Run)

نحن نستخدم **Vite** كسيرفر محلي سريع جداً. 
1. **التشغيل:** السيرفر مضبوط ليعمل حصراً على البورت `5178`.
2. **الأوامر:** يمكنك تشغيل `npm run dev` في الطرفية (Terminal)، أو ببساطة النقر المزدوج على ملف `run-visual-tests.bat` في بيئة ويندوز، والذي سيقوم بتشغيل السيرفر وفتح المتصفح تلقائياً على `http://localhost:5178`.
3. لا يوجد مسار `calibration/`، السيرفر يفتح المجلد الرئيسي (Root) حيث يوجد `index.html`.

---

## 🛠️ 4. كيف تبني مكوناً جديداً؟ (Workflow)

إذا طُلب منك بناء مكون جديد (مثلاً: حقول الإدخال Forms):
1. **قراءة الدستور:** اقرأ ملف `KAIROS-CONSTITUTION.md` لتتأكد من تطبيق القوانين الهندسية (لا حواف دائرية، استخدام الألوان الدلالية فقط، وتوليد كافة الحالات).
2. **كتابة الـ CSS:** قم بإنشاء أو تعديل الملف الخاص بالمكون داخل `src/components/` (مثلاً `forms.css`).
3. **بناء ملف الـ HTML:** قم بإنشاء ملف `forms.html` داخل مجلد `showcase-data/`. اكتب بداخله حالات المكون المختلفة (مثال: حقل نصي، حقل بحث، حقل معطل، إلخ) مستخدماً الـ Utilities المسموحة فقط.
4. **تسجيل المكون:** افتح `showcase.js`، وأضف الـ Component الجديد إلى مصفوفة `KairosInventory` لكي يظهر في القائمة الجانبية تلقائياً (تذكر أن القائمة الجانبية مبنية بأزرار `.kairos-btn`).

---

## 📜 5. القوانين العليا (The Supreme Documents)
لا تقم بأي تعديل هيكلي قبل قراءة هذه الملفات:
- **`KAIROS-CONSTITUTION.md`**: دستور بناء المكونات (يحتوي على قوانين المسافات، معمارية الـ HTML النقي، وحظر أطر العمل داخل النواة).
- **`DEPENDENCY-RULES.md`**: قواعد الاعتماديات المعمارية (كيف تعتمد طبقات الـ CSS على بعضها).

---
*نتمنى لك حظاً موفقاً في بناء واجهات البث التلفزيوني الأكثر احترافية. اتبع الدستور، حافظ على النقاء، وابنِ واجهات لا تقهر!*
