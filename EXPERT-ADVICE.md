# EXPERT-ADVICE.md — نصائح المهندس الرئيسي للمنفذين

> خلاصة القرارات الصحيحة والأخطاء المكلفة من واقع بناء Kairos.
> هذه ليست قواعد إلزامية مثل AGENTS.md — هذه **طريقة التفكير** التي تجعل القواعد منطقية.
> اقرأها مرة قبل أن تكتب أول سطر، وأعد قراءتها حين تشعر أنك "تحيل ذكياً".

---

## 1 · الفلسفة قبل الكود — وتى تتخلى عنها

**DOM مصدر الحقيقة** ليس موضة معمارية؛ إنه رهان على العمر الافتراضي.
أنظمة التحكم التلفزيوني تعيش 10–15 سنة. React قد يموت، Vue قد يموت —
لكن `<button data-state="active">` لن يموت أبداً.

- JS في Kairos **كاتب حالة فقط**: يقرأ نية المستخدم، يكتب `data-state`، يطلق حدثاً. لا أكثر.
- إن وجدت نفسك تخزن حالة في متغير JS ثم تزامنها مع DOM — فقد خرجت عن المعمارية. أعد التصميم.
- **متى تتخلى؟** إذا طُلب SSR أو تطبيق جوال أصلي. لا تجبر المعمارية على كل مشكلة.

---

## 2 · نظام التوكنز هو المنتج الحقيقي

المكوّنات مجرد *نتيجة* لنظام التوكنز. لو حُرق المشروع كله وبقيت
`foundation/` + `components.css`، لبنيتَ نصف النظام من جديد خلال أيام.

- **لا تضيف توكناً إلا له معنى دلالي.** `--kairos-btn-padding-y` معنى؛ `--kairos-btn-padding-top-left-special` ليس توكناً بل لغم.
- **لا قيمة خام "مؤقتة".** المؤقت في البرمجة يدوم عشر سنوات. القيمة الخام اليوم = بحث grep مرهق بعد سنة.
- عند الشك في نمط مكوّن: افتح `radio.css` — أنقى ملف في المشروع. إن لم يشبهه كودك، كودك فيه زيادة.
- قاعدة الثلاثات: أي قيمة تتكرر 3 مرات في 3 مكونات تستحق توكن عقد.

---

## 3 · القيود تصنع الجودة — لا تفاوض عليها بالشعور

`border-radius: 0` والأربعة ألوان ليست قيوداً مقيِّدة — إنها **هوية بصرية**
تميز Kairos عن آلاف الأنظمة الرمادية المتطابقة.

- الـ 300ms ليست رقم اعتباطي: مشغّل البث يريد تأكيداً فورياً؛ أي حركة أطول تُشعره بالنفاق ("هل ضغطت فعلاً؟").
- الاستثناء الوحيد المقبول هو المُوثَّق رسمياً (ticker/marquee). **كل استثناء بلا وثيقة = ثغرة** ستتوسع.
- حين يقترح مصمم "لمسة دائرية لطيفة"، أجبه: افتح أي mixer بث حقيقي — سترى زوايا حادة ومربعات. الواقع الصناعي هو مرجعنا.

---

## 4 · قواعد CSS العملية

1. **ترتيب `@import` في kairos.css عقد معماري** — L0→L9. تغييره يقلب الطبقات فوق بعضها بصمت.
2. **Specificity مسطحة دائماً:** selector واحد لكل قاعدة. `.a .b .c.active span` = رائحة تصميم.
3. **`!important` ممنوع** — باستثناءان موجودان مسبقاً لتقليل الحركة (`prefers-reduced-motion`) ولا جديد قبلهما.
4. لا تستخدم `@layer` الآن — browserslist لدينا أوسع من دعمها الكامل. دوّنها كدين تقني معروف وواضح.
5. الملف > 100 سطر؟ غالباً يخفي مكوّنين متنكرين. افحصه.

---

## 5 · قواعد JS العملية

1. **Delegation واحد على `document` لكل module** — لا مستمع لكل زر. ألف زر = صفر مستمعين إضافيين.
2. **`init()` idempotent دائماً** — دفعنا ثمن غياب ذلك باختبارات تنكسر. حارس `initialized` في كل module جديد، بلا استثناء.
3. **الأحداث القابلة للإلغاء (`before-*`) هي API التوسعة الحقيقية.** المستهلك يوقف سلوكاً بـ `preventDefault()` دون أن نلمس المكتبة. Terminal events (`opened/closed`) للمتابعة لا للمنع.
4. **`waitTransition` لا `setTimeout`.** الأرقام السحرية للمدد تنكسر عند أول تعديل token للحركة.
5. لا تُرجع كائنات حالة من modules — أعد الدوال التي *تغيّر* DOM. القراءة عبر `State.read()`.

---

## 6 · الاختبار الذكي في نظام بصري

- **اختبر السلوك لا البكسل:** `data-state`, ARIA, الأحداث القابلة للإلغاء, التنقل بلوحة المفاتيح.
- اختبار يتحقق من `color: #22C55E` اختبار سيئ — ينكسر مع أول تخصيص عميل ولا يحمي شيئاً.
- اختبار يمنع انكسار Escape داخل modal اختبار ممتاز — يحمي مشغلاً حقيقياً من شاشة محبوسة.
- **axe accessibility منذ اليوم الأول** (انظر modal.spec.ts). مشغل يعمل 12 ساعة متواصلة ليس رفاهية؛ التنقل بالكيبورد في غرفة تحكم = سلامة تشغيل.

---

## 7 · الإيقاع والانضباط — هنا تسقط معظم المشاريع

- **مكوّن واحد من البداية للنهاية** (CSS → showcase → module → tests → commit)
  أفضل عشر مرات من خمسة مكونات "نصف جاهزة".
- **الأربع بوابات قبل كل commit:** lint نظيف، tsc نظيف، tests خضراء، build ناجح.
  بوابة واحدة حمراء = لا commit. لا "سأمررها وأصلحها لاحقاً".
- **showcase أولاً ليس ترفاً:** إن لم تستطع عرض المكوّن في صفحة حية، فهو غير موجود فعلياً.
- **افصل refactor عن feature.** commit يخلطهما = review أعمى وgit blame ملعون.
- رسالة commit تشرح *لماذا* لا *ماذا* — الـ diff يشرح ماذا وحده.

---

## 8 · روائح تنبيه مبكرة (Smells)

| الرائحة | التشخيص |
|---------|----------|
| توكن معرّف ولا أحد يستهلكه | احذفه — التوكنز مكتبة لا متحف |
| نفس pattern مكرر في 3 modules | رقّه إلى `core/` |
| `data-kairos-*` جديد بلا توثيق في specs | أوقف ووثّق أولاً |
| "سأنظفه في PR قادم" | PR قادم = أبداً |
| ملف showcase يشتغل محلياً فقط | كلاسة مكسورة — grep لها قبل الالتزام |
| وكيل AI يقترح "تبسيط" معمارية DOM-state | ارفض بلطف، أشِر له §1 |

---

## 9 · البث المباشر ليس ويب عادي — خصائص المجال

1. **المصمم ليس المستخدم.** المستخدم مشغّل تحت ضغط بث حي، إضاءة منخفضة، نظر مثقل، ويد على switcher مادي.
2. **كل حالة تُميَّز من مترين.** إن احتجت اقتراب وجهك لتمييز PGM من PVW — التصميم فشل.
3. **الأحمر (PGM) مقدّس.** يعني "على الهواء الآن". استعماله زينة = خطر بث حقيقي.
4. **لا شيء يحجب الخيط الرئيسي.** ticker يتحرك بـ `transform` حصراً (GPU) لأسباب تشغيلية لا جمالية.
5. الخطأ المتسامح: نقرة خاطئة يجب أن تكون قابلة للتراجع (undo/take-back) — صمّم المسارات هكذا حين تصل للمكونات الحرجة.

---

## 10 · القرارات المؤجلة عمداً — لا تبنيها الآن

| مؤجل | لماذا |
|------|-------|
| ثيمات متعددة | Dark فقط هو الهوية. Light ممنوع دستورياً حتى ينتهك |
| دعم RTL منهجي | القرار موجود لكنه يحتاج logical properties تدريجياً — عند أول عميل عربي حقيقي |
| `@layer` cascade | بانتظار نطاق browserslist |
| SSR / Portals | خارج نطاق نظام تصميم |
| Figma tokens sync | حين يطلبها فريق تصميم فعلي |

**ابنِ للمستقبل القريب الموثّق، لا للمستقبل الخيالي.**

---

## 11 · العمل مع وكلاء الذكاء الاصطناعي على هذا المستودع

1. `AI-CONTEXT.md` هو **العقد** — حدّثه مع كل تغيير معماري وإلا خدعت الوكيل القادم.
2. كلّف الوكيل **بدورة واحدة كاملة** (§10 في AI-CONTEXT): مكوّن واحد → بوابات → commit.
   لا تقل له "أكمل كل شيء" — سيسلمك 30 ملفاً نصف مختبرة.
3. **راجع الـ diff دائماً قبل الدمج.** الوكيل يميل لحذف ما يراه "زائداً"
   (fallbacks, contracts, تعليقات عربية) وهي غالباً الجوهر.
4. امنحه حرية الاختبارات وشدّده في القواعد الصامتة (tokens, naming) —
   الاختبارات تكشف أخطاءه، والـ linter يكشف انحرافاته.

---

## 12 · الخلاصة في خمسة أسطر

1. العقود (tokens) هي المنتج؛ المكوّنات عملاؤها.
2. DOM يحمل الحقيقة، JS يكتبها، CSS يرسمها — لا عبور للحدود.
3. قيد موثّق خيرٌ من حرية فوضوية؛ استثناء بلا وثيقة ثغرة.
4. مكوّن مكتمل ببواباته الأربع خير من عشرة شبه جاهزة.
5. نحن نبني ليدٍ تهتز تحت ضغط البث الحي — كل تفصيلة قرار تشغيلي.

---
*اكتبها على الحائط إن أحببت. وخاصة البند 5.*
